# --- Web framework ---
fastapi==0.118.0
uvicorn[standard]==0.34.0

# --- Base de datos (SQLAlchemy 2.0 async + PostgreSQL) ---
SQLAlchemy[asyncio]==2.0.36
asyncpg==0.30.0
alembic==1.14.0

# --- Validación / settings (Pydantic v2) ---
pydantic==2.10.4
pydantic-settings==2.7.0
email-validator==2.2.0

# --- Auth (JWT + hashing) ---
PyJWT==2.10.1
passlib==1.7.4
# bcrypt fijado <4.1 por incompatibilidad conocida de passlib 1.7.4 con bcrypt 4.1+
bcrypt==4.0.1

# --- HTTP async (integración TMDB) ---
httpx==0.28.1

# --- Tests ---
pytest==8.3.4
pytest-asyncio==0.25.0
